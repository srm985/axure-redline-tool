import React from 'react';

import ModalComponent from '../../components/ModalComponent';

import './styles.scss';

const SharingLinksModule = (props) => {
    return (
        <ModalComponent>test</ModalComponent>
    );
};

export default SharingLinksModule;
